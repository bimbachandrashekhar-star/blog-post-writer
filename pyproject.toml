[project]
name = "new_blog"
version = "0.1.0"
description = "new-blog using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.1"
]

[project.scripts]
new_blog = "new_blog.main:run"
run_crew = "new_blog.main:run"
train = "new_blog.main:train"
replay = "new_blog.main:replay"
test = "new_blog.main:test"
run_with_trigger = "new_blog.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
